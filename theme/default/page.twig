{% extends 'base.twig' %}

{% block title %}{{ page.title }}{% endblock %}

{% block content %}
<article>
    <h1>{{ page.title }}</h1>
    <div class="page-content">
        {{ page.content|raw }}
    </div>
</article>
{% endblock %}
