<header>
    <nav>
        <ul class="main-nav">
            {% for item in nav %}
                <li>
                    <a href="/{{ item.slug }}">{{ item.title }}</a>
                </li>
            {% endfor %}

        </ul>
    </nav>
</header>
